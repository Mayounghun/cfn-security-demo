rule instance_type_restriction when type == "AWS::EC2::Instance" {
    Properties.InstanceType in ["t3.micro", "t3.small"]
}

rule must_have_owner_tag when type == "AWS::EC2::Instance" {
    Properties.Tags.*.Key contains ["Owner"]
}
