rule check_instance_type {
    Resources.*[ Type == "AWS::EC2::Instance" ].Properties.InstanceType == "t3.micro"
}

rule check_owner_tag {
    Resources.*[ Type == "AWS::EC2::Instance" ].Properties.Tags.*[ Key == "Owner" ]
}
